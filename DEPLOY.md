Deploy:
unzip -o RobloxMID-FINAL-REAL.zip
docker compose down
docker compose up -d --build

Changes:
- Editor positioned below Upload Track
- Dark UI matching dashboard
- File selection trigger
- Removed plain default controls
