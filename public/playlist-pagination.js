/* RobloxMID playlist pagination UI. Loaded after app.js by the /app.js wrapper. */
(function(){
  const $=s=>document.querySelector(s),u=$("#urlInput"),info=$("#urlInfoBtn"),card=$("#playlistCard"),list=$("#playlistItems"),title=$("#playlistTitle"),meta=$("#playlistMeta"),dl=$("#playlistDownloadBtn"),all=$("#playlistSelectAll"),prog=$("#playlistProgress"),bar=$("#playlistProgBar"),txt=$("#playlistProgText"),preview=$("#urlPreviewCard"),status=$("#urlStatus"),statusText=$("#urlStatusText");
  if(!u||!info||!card||!list)return;
  const SIZE=50;let url="",page=1,cap=100,next=false,busy=false,items=[],selected=new Set(),selectedItems=new Map();
  const esc=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"}[c]));
  const setStatus=(msg,error=false)=>{if(status&&statusText){status.className=`url-status ${error?'error':'info'}`;status.classList.remove('hidden');statusText.textContent=msg}};
  function css(){if($("#plPagerCss"))return;const s=document.createElement("style");s.id="plPagerCss";s.textContent=`#playlistCard{margin-top:14px;padding:14px;border:1px solid var(--line);border-radius:16px;background:rgba(255,255,255,.025)}#playlistCard .playlist-head{display:flex;align-items:center;justify-content:space-between;gap:12px;padding-bottom:12px;border-bottom:1px solid var(--line)}#playlistCard .playlist-actions{display:flex;gap:7px;flex-wrap:wrap;align-items:center}#playlistCard .playlist-items{display:flex;flex-direction:column;gap:6px;margin-top:11px;max-height:520px;overflow:auto;padding-right:3px}#playlistCard .playlist-item{display:grid;grid-template-columns:20px 44px minmax(0,1fr);gap:10px;align-items:center;padding:8px;border:1px solid transparent;border-radius:12px;background:#ffffff03;transition:.15s}#playlistCard .playlist-item:hover{border-color:var(--line);background:#ffffff06}#playlistCard .playlist-item input{accent-color:#8b5cf6}#playlistCard .pl-thumb{width:44px;height:44px;border-radius:9px;object-fit:cover;background:#0b1020}#playlistCard .pl-meta{min-width:0}.pl-title{font-size:12px;font-weight:700;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.pl-sub{font-size:10px;color:var(--muted);margin-top:2px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.playlist-pagination{display:flex;justify-content:space-between;align-items:center;gap:10px;flex-wrap:wrap;padding:12px 0 2px;margin-top:10px;border-top:1px solid var(--line)}.playlist-page-actions{display:flex;align-items:center;gap:7px;flex-wrap:wrap}.playlist-page-info{font-size:10px;color:var(--muted)}.playlist-page-btn,.playlist-limit{border:1px solid var(--line);background:#ffffff05;color:var(--text);border-radius:9px;padding:7px 10px;font-size:11px;font-weight:700;cursor:pointer}.playlist-page-btn:disabled{opacity:.4;cursor:not-allowed}.playlist-limit{outline:none}.playlist-selection-count{font-size:10px;color:#a78bfa;font-weight:800;margin-top:3px}@media(max-width:700px){.playlist-pagination{align-items:flex-start;flex-direction:column}.playlist-page-actions{width:100%}.playlist-page-btn{flex:1}}`;document.head.appendChild(s)}
  function controls(){let p=$("#playlistPagination");if(p)return p;p=document.createElement("div");p.id="playlistPagination";p.className="playlist-pagination";p.innerHTML='<div><div class="playlist-page-info" id="playlistPageInfo"></div><div class="playlist-selection-count" id="playlistSelectionCount">0 dipilih</div></div><div class="playlist-page-actions"><select class="playlist-limit" id="playlistLimit"><option value="100">100 track</option><option value="500">500 track</option><option value="0">Semua track</option></select><button type="button" class="playlist-page-btn" id="playlistPrev">← Sebelumnya</button><button type="button" class="playlist-page-btn" id="playlistNext">Berikutnya →</button></div>';card.appendChild(p);$("#playlistLimit").onchange=async e=>{cap=Number(e.target.value);page=1;items=[];selected.clear();selectedItems.clear();await load(1,false)};$("#playlistPrev").onclick=async()=>{if(page>1&&!busy)await load(page-1,false)};$("#playlistNext").onclick=async()=>{if(next&&!busy)await load(page+1,false)};return p}
  function update(){if(dl){dl.disabled=!selectedItems.size;dl.textContent=selectedItems.size?`⬇ Download ${selectedItems.size} Track`:"⬇ Download Terpilih"}const i=$("#playlistPageInfo"),sc=$("#playlistSelectionCount"),p=$("#playlistPrev"),n=$("#playlistNext"),s=$("#playlistLimit");if(i)i.textContent=`Halaman ${page} · ${items.length} tampil · batas ${cap===0?'Semua':cap}`;if(sc)sc.textContent=`${selectedItems.size} dipilih`;if(p)p.disabled=busy||page<=1;if(n)n.disabled=busy||!next;if(s)s.value=String(cap)}
  function render(){list.innerHTML=items.map((x,i)=>`<label class="playlist-item" data-pidx="${i}"><input type="checkbox" class="pl-check-paged" data-id="${esc(x.id)}" ${selected.has(String(x.id))?'checked':''}><img class="pl-thumb" src="${esc(x.thumbnail||'')}" alt="" onerror="this.style.display='none'"><div class="pl-meta"><div class="pl-title">${esc(x.title)}</div><div class="pl-sub">${esc(x.uploader||'')}${x.duration_string?' · ⏱ '+esc(x.duration_string):''}</div></div></label>`).join('');list.querySelectorAll('.pl-check-paged').forEach(c=>c.onchange=()=>{const id=String(c.dataset.id),item=items.find(x=>String(x.id)===id);if(c.checked){selected.add(id);if(item)selectedItems.set(id,item)}else{selected.delete(id);selectedItems.delete(id)}update()});update()}
  async function load(p,append){busy=true;update();try{const q=new URLSearchParams({url,page:String(p),pageSize:String(SIZE),maxItems:String(cap)});const r=await fetch(`/api/playlist-info?${q}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({url})});const d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.error||'Gagal mengambil playlist.');if(!d.isPlaylist&&d.items?.length===1){const x=d.items[0];preview?.classList.remove('hidden');const th=$("#urlThumb"),tt=$("#urlTitle"),up=$("#urlUploader"),du=$("#urlDuration"),fb=$("#urlFetchBtn");if(th){th.src=x.thumbnail||'';th.classList.toggle('hidden',!x.thumbnail)}if(tt)tt.textContent=x.title||'Track';if(up)up.textContent=x.uploader||'';if(du)du.textContent=x.duration_string?`⏱ ${x.duration_string}`:'';if(fb)fb.disabled=false;card.classList.add('hidden');next=false;return}const incoming=d.items||[];incoming.forEach(x=>{if(selected.has(String(x.id)))selectedItems.set(String(x.id),x)});items=append?[...items,...incoming]:incoming;page=Number(d.page||p);next=!!d.hasNext;title.textContent=d.playlistTitle||'Playlist';meta.textContent=cap===0?`${items.length}${next?'+':''} track dimuat`:`${items.length}/${cap} track dimuat`;card.classList.remove('hidden');preview?.classList.add('hidden');render()}catch(e){card.classList.add('hidden');setStatus('✗ '+(e.message||'Gagal mengambil playlist.'),true)}finally{busy=false;update()}}
  async function check(){url=u.value.trim();if(!url)return;page=1;items=[];selected.clear();selectedItems.clear();css();controls();if(preview)preview.classList.add('hidden');setStatus('⏳ Memuat playlist...');await load(1,false);if(!busy&&status)status.classList.add('hidden')}
  all&&(all.onclick=()=>{const c=list.querySelectorAll('.pl-check-paged'),on=[...c].some(x=>!x.checked);c.forEach(x=>{x.checked=on;const id=String(x.dataset.id),item=items.find(v=>String(v.id)===id);if(on){selected.add(id);if(item)selectedItems.set(id,item)}else{selected.delete(id);selectedItems.delete(id)}});update()});
  dl&&(dl.onclick=async()=>{if(!selectedItems.size||!url||busy)return;while(next&&(cap===0||items.length<cap)){const before=page;await load(page+1,true);if(page===before)break}const chosen=[...selectedItems.values()];if(!chosen.length)return;dl.disabled=true;all&&(all.disabled=true);prog?.classList.remove('hidden');const start=_downloadQueue.length;chosen.forEach(x=>_downloadQueue.push({id:x.id,title:x.title,thumbnail:x.thumbnail,url:x.webpage_url,file:null,status:'waiting'}));renderQueue();for(let i=0;i<chosen.length;i++){const qi=start+i;if(txt)txt.textContent=`Mendownload ${i+1}/${chosen.length}: ${chosen[i].title}`;if(bar)bar.style.width=(i/chosen.length*100)+'%';await downloadQueueItem(qi)}if(bar)bar.style.width='100%';if(txt)txt.textContent=`✓ ${chosen.length} track masuk antrian — edit & upload satu per satu!`;dl.disabled=false;all&&(all.disabled=false);update()});
  info.onclick=check;
})();
