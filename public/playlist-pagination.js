/* RobloxMID playlist pagination UI. Loaded after app.js by the /app.js wrapper. */
(function(){
  const $=s=>document.querySelector(s), u=$("#urlInput"), info=$("#urlInfoBtn"), card=$("#playlistCard"), list=$("#playlistItems"), title=$("#playlistTitle"), meta=$("#playlistMeta"), dl=$("#playlistDownloadBtn"), all=$("#playlistSelectAll"), prog=$("#playlistProgress"), bar=$("#playlistProgBar"), txt=$("#playlistProgText"), preview=$("#urlPreviewCard");
  if(!u||!info||!card||!list)return;
  const SIZE=50;let url="",page=1,cap=100,next=false,busy=false,items=[],selected=new Set();
  const esc=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"}[c]));
  function css(){if($("#plPagerCss"))return;const s=document.createElement("style");s.id="plPagerCss";s.textContent='.playlist-pagination{display:flex;justify-content:space-between;align-items:center;gap:10px;flex-wrap:wrap;padding:12px 0 2px;margin-top:10px;border-top:1px solid var(--line)}.playlist-page-actions{display:flex;align-items:center;gap:7px;flex-wrap:wrap}.playlist-page-info{font-size:11px;color:var(--muted)}.playlist-page-btn,.playlist-limit{border:1px solid var(--line);background:#ffffff05;color:var(--text);border-radius:9px;padding:7px 10px;font-size:11px;font-weight:700;cursor:pointer}.playlist-page-btn:disabled{opacity:.4;cursor:not-allowed}.playlist-limit{outline:none}';document.head.appendChild(s)}
  function controls(){let p=$("#playlistPagination");if(p)return p;p=document.createElement("div");p.id="playlistPagination";p.className="playlist-pagination";p.innerHTML='<div class="playlist-page-info" id="playlistPageInfo"></div><div class="playlist-page-actions"><select class="playlist-limit" id="playlistLimit"><option value="100">100 track</option><option value="500">500 track</option><option value="0">Semua track</option></select><button type="button" class="playlist-page-btn" id="playlistPrev">← Sebelumnya</button><button type="button" class="playlist-page-btn" id="playlistNext">Berikutnya →</button></div>';card.appendChild(p);$("#playlistLimit").onchange=async e=>{cap=Number(e.target.value);page=1;items=[];selected.clear();await load(1,false)};$("#playlistPrev").onclick=async()=>{if(page>1&&!busy){page--;await load(page,false)}};$("#playlistNext").onclick=async()=>{if(next&&!busy){page++;await load(page,false)}};return p}
  function update(){if(dl){dl.disabled=!selected.size;dl.textContent=selected.size?`⬇ Download ${selected.size} Track`:"⬇ Download Terpilih"}const i=$("#playlistPageInfo"),p=$("#playlistPrev"),n=$("#playlistNext"),s=$("#playlistLimit");if(i)i.textContent=`Halaman ${page} · ${items.length} track tampil · batas ${cap===0?'Semua':cap}`;if(p)p.disabled=busy||page<=1;if(n)n.disabled=busy||!next;if(s)s.value=String(cap)}
  function render(){list.innerHTML=items.map((x,i)=>`<label class="playlist-item" data-pidx="${i}"><input type="checkbox" class="pl-check-paged" data-id="${esc(x.id)}" ${selected.has(String(x.id))?'checked':''}><img class="pl-thumb" src="${esc(x.thumbnail||'')}" alt="" onerror="this.style.display='none'"><div class="pl-meta"><div class="pl-title">${esc(x.title)}</div><div class="pl-sub">${esc(x.uploader||'')}${x.duration_string?' · ⏱ '+esc(x.duration_string):''}</div></div></label>`).join('');list.querySelectorAll('.pl-check-paged').forEach(c=>c.onchange=()=>{const id=String(c.dataset.id);c.checked?selected.add(id):selected.delete(id);update()});update()}
  async function load(p,append){busy=true;update();try{const q=new URLSearchParams({url,page:String(p),pageSize:String(SIZE),maxItems:String(cap)});const r=await fetch(`/api/playlist-info?${q}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({url})});const d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.error||'Gagal mengambil playlist.');if(!d.isPlaylist&&d.items?.length===1){const x=d.items[0];preview?.classList.remove('hidden');const th=$("#urlThumb"),tt=$("#urlTitle"),up=$("#urlUploader"),du=$("#urlDuration"),fb=$("#urlFetchBtn");if(th){th.src=x.thumbnail||'';th.classList.toggle('hidden',!x.thumbnail)}if(tt)tt.textContent=x.title||'Track';if(up)up.textContent=x.uploader||'';if(du)du.textContent=x.duration_string?`⏱ ${x.duration_string}`:'';if(fb)fb.disabled=false;card.classList.add('hidden');next=false;return}items=append?[...items,...(d.items||[])]:d.items||[];page=Number(d.page||p);next=!!d.hasNext;title.textContent=d.playlistTitle||'Playlist';meta.textContent=cap===0?`${items.length}${next?'+':''} track dimuat`:`${items.length}/${cap} track dimuat`;card.classList.remove('hidden');preview?.classList.add('hidden');render()}catch(e){card.classList.add('hidden');alert(e.message||'Gagal mengambil playlist.')}finally{busy=false;update()}}
  async function check(){url=u.value.trim();if(!url)return;page=1;items=[];selected.clear();css();controls();if(preview)preview.classList.add('hidden');await load(1,false)}
  all&&(all.onclick=()=>{const c=list.querySelectorAll('.pl-check-paged'),on=[...c].some(x=>!x.checked);c.forEach(x=>{x.checked=on;const id=String(x.dataset.id);on?selected.add(id):selected.delete(id)});update()});
  dl&&(dl.onclick=async()=>{if(!selected.size||!url||busy)return;while(next&&(cap===0||items.length<cap)){page++;await load(page,true)}const chosen=items.filter(x=>selected.has(String(x.id)));if(!chosen.length)return;dl.disabled=true;all&&(all.disabled=true);prog?.classList.remove('hidden');const start=_downloadQueue.length;chosen.forEach(x=>_downloadQueue.push({id:x.id,title:x.title,thumbnail:x.thumbnail,url:x.webpage_url,file:null,status:'waiting'}));renderQueue();for(let i=0;i<chosen.length;i++){const qi=start+i;if(txt)txt.textContent=`Mendownload ${i+1}/${chosen.length}: ${chosen[i].title}`;if(bar)bar.style.width=(i/chosen.length*100)+'%';await downloadQueueItem(qi)}if(bar)bar.style.width='100%';if(txt)txt.textContent=`✓ ${chosen.length} track masuk antrian — edit & upload satu per satu!`;dl.disabled=false;all&&(all.disabled=false)});
  info.onclick=check;
})();
