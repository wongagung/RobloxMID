const TYPES={image:{label:'Image',icon:'▧',assetType:'Image',accept:['.png','.jpg','.jpeg','.bmp','.tga']},decal:{label:'Decal',icon:'◈',assetType:'Decal',accept:['.png','.jpg','.jpeg','.bmp','.tga']},model:{label:'Model',icon:'◇',assetType:'Model',accept:['.fbx','.gltf','.glb','.rbxm','.rbxmx']},animation:{label:'Animation',icon:'◎',assetType:'Animation',accept:['.rbxm','.rbxmx']},audio:{label:'Audio',icon:'♫',assetType:'Audio',accept:['.mp3','.wav','.ogg','.flac']},video:{label:'Video',icon:'▶',assetType:'Video',accept:['.mp4','.mov','.webm']}};
const state={type:'auto',files:[],uploading:false,paused:false,filter:'all',search:'',running:0,concurrency:2,workspace:'General'};
const $=id=>document.getElementById(id);const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
const ext=name=>{const x=String(name).toLowerCase();const i=x.lastIndexOf('.');return i>=0?x.slice(i):''};
const fmt=n=>n<1048576?`${(n/1024).toFixed(1)} KB`:`${(n/1048576).toFixed(2)} MB`;
const dateToken=()=>new Date().toISOString().slice(0,10);
function toast(msg,kind=''){const el=$('toast');el.textContent=msg;el.className=`toast ${kind} show`;clearTimeout(toast.timer);toast.timer=setTimeout(()=>el.className='toast',3200)}
function getHistory(){try{return JSON.parse(localStorage.getItem('roblox_asset_hub_history')||'[]')}catch{return[]}}
function saveHistory(v){localStorage.setItem('roblox_asset_hub_history',JSON.stringify(v.slice(0,300)))}
function typeForFile(file){const e=ext(file.name);for(const [key,cfg] of Object.entries(TYPES))if(cfg.accept.includes(e))return key;return null}
async function hashFile(file){const buf=await file.arrayBuffer();const digest=await crypto.subtle.digest('SHA-256',buf);return [...new Uint8Array(digest)].map(b=>b.toString(16).padStart(2,'0')).join('')}
function fileType(item){return state.type==='auto'?(item.detectedType||typeForFile(item.file)||'image'):state.type}
function templateName(item,index){const prefix=$('assetName').value.trim();const base=item.file.name.replace(/\.[^.]+$/,'').replace(/[_-]+/g,' ').trim();const tpl=$('nameTemplate')?.value.trim()||'{prefix} - {filename}';const name=tpl.replaceAll('{prefix}',prefix||'Asset').replaceAll('{filename}',base).replaceAll('{index}',String(index+1).padStart(3,'0')).replaceAll('{date}',dateToken());return name.slice(0,50)}
function updateType(type){state.type=type;document.querySelectorAll('.type-card').forEach(b=>b.classList.toggle('active',b.dataset.type===type));fileInput.accept=type==='auto'?Object.values(TYPES).flatMap(x=>x.accept).filter((v,i,a)=>a.indexOf(v)===i).join(','):TYPES[type].accept.join(',');$('dropTitle').textContent=type==='auto'?'Drop files here':`Drop your ${TYPES[type].label.toLowerCase()} files here`;$('dropMeta').textContent=type==='auto'?'Auto detection enabled · 20 MB maximum per file':`${TYPES[type].accept.map(x=>x.slice(1).toUpperCase()).join(' · ')} · 20 MB maximum`;renderQueue();renderPreviews();updateButton()}
async function addFiles(list){const files=[...list||[]];let bad=0,dup=0;const existingHashes=new Set(state.files.map(x=>x.hash).filter(Boolean));for(const file of files){const detected=typeForFile(file);const target=state.type==='auto'?detected:state.type;if(!detected||!TYPES[target]||!TYPES[target].accept.includes(ext(file.name))||file.size>20*1024*1024){bad++;continue}const key=`${file.name}|${file.size}|${file.lastModified}`;if(state.files.some(x=>x.key===key)){dup++;continue}let hash='';try{hash=await hashFile(file)}catch{}const duplicateHistory=getHistory().some(x=>x.sha256&&x.sha256===hash);if(existingHashes.has(hash)){dup++;continue}const item={key,file,detectedType:detected,hash,status:duplicateHistory?'duplicate':'queued',percent:0,result:null,error:null};state.files.push(item);existingHashes.add(hash)}if(bad)toast(`${bad} file ditolak: format atau ukuran tidak sesuai.`,'error');if(dup)toast(`${dup} file duplikat dilewati.`);renderQueue();renderPreviews();updateButton()}
function clearQueue(){if(state.uploading)return;state.files=[];fileInput.value='';renderQueue();renderPreviews();updateButton()}
function removeFile(key){if(state.uploading)return;state.files=state.files.filter(x=>x.key!==key);renderQueue();renderPreviews();updateButton()}
function renderPreviews(){const box=$('previewStrip');if(!box)return;if(!state.files.length){box.innerHTML='';return}box.innerHTML=state.files.slice(0,12).map((x,i)=>{const t=fileType(x);const src=(t==='image'&&URL.createObjectURL)?URL.createObjectURL(x.file):'';return `<div class="preview-item">${src?`<img src="${src}" alt="">`:`<div style="height:70px;display:grid;place-items:center;color:#777;font-size:22px">${TYPES[t]?.icon||'◌'}</div>`}<button class="remove-preview" data-rm="${esc(x.key)}">×</button><div class="preview-label">${esc(x.file.name)}</div></div>`}).join('');box.querySelectorAll('[data-rm]').forEach(b=>b.onclick=()=>removeFile(b.dataset.rm))}
function renderQueue(){const list=$('queueList');if(!list)return;const ready=state.files.filter(x=>x.status==='queued').length,running=state.files.filter(x=>x.status==='uploading').length,done=state.files.filter(x=>['done','processing'].includes(x.status)).length,failed=state.files.filter(x=>x.status==='error').length;['queueCount','queueReady','queueRunning','queueDone','queueErrors'].forEach((id,i)=>{if($(id))$(id).textContent=[state.files.length,ready,running,done,failed][i]});$('selectionSummary').textContent=`${state.files.length} file${state.files.length===1?'':'s'} selected`;if(!state.files.length){list.innerHTML='<div class="empty-history"><h3>Queue is empty</h3><p>Add files above to build a batch.</p></div>';return}list.innerHTML=state.files.map((x,i)=>{const t=fileType(x),cfg=TYPES[t],name=templateName(x,i),id=x.result?.assetId||'',url=x.result?.assetUrl||(id?`https://create.roblox.com/store/asset/${id}`:'');return `<div class="queue-item"><div class="queue-icon">${cfg.icon}</div><div class="queue-main"><strong>${esc(name)}</strong><small>${esc(x.file.name)} · ${esc(cfg.label)} · ${fmt(x.file.size)} · ${esc(x.status)}</small>${x.error?`<em>${esc(x.error)}</em>`:''}</div><div class="queue-progress"><i style="width:${x.percent}%"></i></div><div class="queue-actions">${id?`<button data-open="${esc(url)}">Open</button><button data-copy="${esc(id)}">Copy</button><button data-code="${esc(id)}" data-code-type="${esc(t)}">Code</button>`:''}${!state.uploading?`<button data-rm="${esc(x.key)}">×</button>`:''}</div></div>`}).join('');list.querySelectorAll('[data-rm]').forEach(b=>b.onclick=()=>removeFile(b.dataset.rm));list.querySelectorAll('[data-copy]').forEach(b=>b.onclick=()=>copyText(b.dataset.copy,'Asset ID copied.'));list.querySelectorAll('[data-open]').forEach(b=>b.onclick=()=>window.open(b.dataset.open,'_blank','noopener'));list.querySelectorAll('[data-code]').forEach(b=>b.onclick=()=>openCode(b.dataset.code,b.dataset.codeType))}
async function uploadOne(item,index){item.status='uploading';item.percent=8;renderQueue();const t=fileType(item),fd=new FormData();fd.append('file',item.file,item.file.name);fd.append('assetType',TYPES[t].assetType);fd.append('displayName',templateName(item,index));fd.append('description',$('description').value.trim());fd.append('creatorType',$('creatorType').value);if($('creatorType').value==='group')fd.append('groupId',$('groupId').value.trim());try{const r=await fetch('/api/assets/upload',{method:'POST',body:fd});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||d.message||`HTTP ${r.status}`);item.result=d;item.percent=100;item.status=d.status==='processing'?'processing':'done';const h=getHistory();h.unshift({type:t,name:d.displayName||templateName(item,index),assetId:d.assetId||'',status:d.status||'completed',operationId:d.operationId||null,createdAt:new Date().toISOString(),sha256:item.hash,workspace:state.workspace});saveHistory(h);renderHistory()}catch(e){item.status='error';item.error=e.message||'Upload failed';item.percent=0}renderQueue()}
function pump(){while(!state.paused&&state.running<state.concurrency){const idx=state.files.findIndex(x=>['queued','retry'].includes(x.status));if(idx<0)break;const item=state.files[idx];state.running++;uploadOne(item,idx).finally(()=>{state.running--;pump();finishMaybe()})}}
function finishMaybe(){if(state.uploading&&state.running===0&&!state.files.some(x=>['queued','retry','uploading'].includes(x.status))){state.uploading=false;$('uploadAll').disabled=false;toast(`${state.files.filter(x=>['done','processing'].includes(x.status)).length} selesai · ${state.files.filter(x=>x.status==='error').length} gagal`,state.files.some(x=>x.status==='error')?'error':'success');renderQueue();updateButton()}}
function startUpload(){if(state.uploading||!state.files.length)return;if($('creatorType').value==='group'&&!/^\d+$/.test($('groupId').value.trim())){toast('Group ID wajib diisi.','error');return}const invalid=state.files.some(x=>!TYPES[fileType(x)]);if(invalid){toast('Ada file dengan tipe yang tidak didukung.','error');return}state.concurrency=Math.min(5,Math.max(1,Number($('concurrency')?.value||2)));state.uploading=true;state.paused=false;$('uploadAll').disabled=true;pump()}
function retryFailed(){state.files.filter(x=>x.status==='error').forEach(x=>{x.status='retry';x.error=null;x.percent=0});if(!state.uploading){if(state.files.some(x=>x.status==='retry'))startUpload()}else pump();renderQueue()}
function copyText(value,msg='Copied.'){if(!value)return;navigator.clipboard?.writeText(value).then(()=>toast(msg,'success')).catch(()=>{toast(value)})}
function assetCode(id,type){const uri=`rbxassetid://${id}`;switch(type){case'image':case'decal':return `local assetId = "${uri}"`;case'audio':return `local sound = Instance.new("Sound")\nsound.SoundId = "${uri}"\nsound.Parent = workspace`;case'animation':return `local animation = Instance.new("Animation")\nanimation.AnimationId = "${uri}"`;default:return `local ASSET_ID = "${uri}"`}}
function openCode(id,type){openModal('DEVELOPER TOOL','Roblox code snippet',`<div class="codebox"><textarea id="codeArea" readonly>${esc(assetCode(id,type))}</textarea></div><div class="modal-actions"><button class="secondary-btn" id="copyCode">Copy Lua</button><button class="primary-btn" id="closeCode">Done</button></div>`);$('copyCode').onclick=()=>copyText(assetCode(id,type),'Lua snippet copied.');$('closeCode').onclick=closeModal}
async function inspect(id,focus=true){if(!id)return;$('inspectorTitle').textContent=`Asset #${id}`;$('inspectorBody').innerHTML='<div class="inspector-loading">Loading metadata…</div>';try{const r=await fetch(`/api/assets/${encodeURIComponent(id)}`);const d=await r.json();if(!r.ok)throw new Error(d.error||d.message||`HTTP ${r.status}`);const a=d.asset||d,type=String(a.assetType||'').toLowerCase().replace(/^asset_type_/,'');const t=Object.keys(TYPES).find(k=>String(TYPES[k].assetType).toLowerCase()===type)||'image';$('inspectorBody').innerHTML=`<div class="inspector-grid"><div><span>Name</span><b>${esc(a.displayName||a.name||'—')}</b></div><div><span>Type</span><b>${esc(a.assetType||'—')}</b></div><div><span>Moderation</span><b>${esc(a.moderationResult?.moderationState||'—')}</b></div><div><span>Revision</span><b>${esc(a.revisionId||'—')}</b></div></div><div class="inspector-links"><button id="copyI">Copy ID</button><button id="copyU">Copy URI</button><button id="openR">Open Roblox ↗</button><button id="codeR">Generate Lua</button></div><div class="codebox"><textarea readonly>${esc(assetCode(id,t))}</textarea></div>`;$('copyI').onclick=()=>copyText(id,'Asset ID copied.');$('copyU').onclick=()=>copyText(`rbxassetid://${id}`,'URI copied.');$('openR').onclick=()=>window.open(`https://create.roblox.com/store/asset/${encodeURIComponent(id)}`,'_blank','noopener');$('codeR').onclick=()=>openCode(id,t)}catch(e){$('inspectorBody').innerHTML=`<div class="inspector-error">${esc(e.message||e)}</div>`}if(focus)$('inspectorPanel').scrollIntoView({behavior:'smooth',block:'start'})}
function renderHistory(){const all=getHistory();const term=state.search.toLowerCase();const items=all.filter(x=>(state.filter==='all'||x.type===state.filter)&&`${x.name} ${x.assetId||''} ${x.workspace||''}`.toLowerCase().includes(term));$('metricAssets').textContent=all.length;$('statCompleted').textContent=all.filter(x=>['completed','done'].includes(x.status)).length;$('statReviewing').textContent=all.filter(x=>!['completed','done','rejected','failed'].includes(x.status)).length;$('statRejected').textContent=all.filter(x=>/reject|fail/i.test(x.status)).length;$('historyList').innerHTML=items.length?items.map(x=>{const c=TYPES[x.type]||TYPES.image,id=x.assetId;return `<div class="history-row"><div class="history-art">${c.icon}</div><div class="history-main"><strong>${esc(x.name)}</strong><small>${esc(x.workspace||'General')} · ${x.createdAt?new Date(x.createdAt).toLocaleString('id-ID',{dateStyle:'medium',timeStyle:'short'}):''}</small></div><div class="history-type">${c.label}</div><div class="history-status">${statusLabel(x.status)}</div><div class="history-id">${esc(id||'—')}</div><button class="history-open" ${id?'':'disabled'} data-open="${id?`https://create.roblox.com/store/asset/${id}`:''}">Open</button><button class="history-copy" data-id="${esc(id||'')}">Copy</button><button class="history-code" data-code="${esc(id||'')}" data-code-type="${esc(x.type)}">Lua</button></div>`}).join(''):'<div class="empty-history"><h3>No matching assets</h3><p>Upload an asset or change your search.</p></div>';$('historyList').querySelectorAll('[data-open]').forEach(b=>b.onclick=()=>b.dataset.open&&window.open(b.dataset.open,'_blank','noopener'));$('historyList').querySelectorAll('[data-id]').forEach(b=>b.onclick=()=>copyText(b.dataset.id,'Asset ID copied.'));$('historyList').querySelectorAll('[data-code]').forEach(b=>b.onclick=()=>openCode(b.dataset.code,b.dataset.codeType))}
function statusLabel(s){return /reject|fail/i.test(s)?'Rejected':/complete|done/i.test(s)?'Approved':'Reviewing'}
function exportManifest(){const rows=getHistory();const payload={version:1,generatedAt:new Date().toISOString(),workspace:state.workspace,assets:rows};const blob=new Blob([JSON.stringify(payload,null,2)],{type:'application/json'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`roblox-assets-${dateToken()}.json`;a.click();URL.revokeObjectURL(a.href)}
function exportLua(){const rows=getHistory().filter(x=>x.assetId);const out=['local Assets = {',...rows.map(x=>`  ${x.name.replace(/[^A-Za-z0-9_]/g,'_').replace(/^\d+/,'_')} = "rbxassetid://${x.assetId}",`),'}','', 'return Assets'].join('\n');const blob=new Blob([out],{type:'text/plain'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`RobloxAssets-${dateToken()}.lua`;a.click()}
function openModal(eyebrow,title,body){$('modalEyebrow').textContent=eyebrow;$('modalTitle').textContent=title;$('modalBody').innerHTML=body;$('modal').classList.remove('hidden')}
function closeModal(){$('modal').classList.add('hidden')}
function workspaceData(){try{return JSON.parse(localStorage.getItem('roblox_asset_hub_workspaces')||'{}')}catch{return{}}}
function saveWorkspace(data){localStorage.setItem('roblox_asset_hub_workspaces',JSON.stringify(data))}
function refreshWorkspace(){const d=workspaceData();if(!d.General)d.General={description:'Your default asset workspace.'};if(!d[state.workspace])state.workspace='General';$('workspaceTitle').textContent=state.workspace;$('workspaceDesc').textContent=d[state.workspace].description||'Asset workspace';$('workspaceLabel').textContent=`Workspace: ${state.workspace}`}
function newWorkspace(){openModal('WORKSPACE','Create workspace','<div class="modal-form"><input id="wsName" maxlength="32" placeholder="e.g. Horror Game"><textarea id="wsDesc" rows="3" placeholder="Short description"></textarea></div><div class="modal-actions"><button class="secondary-btn" id="wsCancel">Cancel</button><button class="primary-btn" id="wsCreate">Create</button></div>');$('wsCancel').onclick=closeModal;$('wsCreate').onclick=()=>{const n=$('wsName').value.trim(),d=$('wsDesc').value.trim();if(!n)return toast('Workspace name wajib diisi.','error');const data=workspaceData();data[n]={description:d||'Asset workspace'};saveWorkspace(data);state.workspace=n;refreshWorkspace();closeModal();renderHistory();toast('Workspace created.','success')}}
function switchWorkspace(){const d=workspaceData();const names=Object.keys(d);openModal('WORKSPACE','Switch workspace',`<div class="modal-list">${names.map(n=>`<button data-ws="${esc(n)}">${esc(n)}<br><small>${esc(d[n].description||'')}</small></button>`).join('')}</div>`);$('modalBody').querySelectorAll('[data-ws]').forEach(b=>b.onclick=()=>{state.workspace=b.dataset.ws;refreshWorkspace();closeModal();renderHistory()})}
function inspectPrompt(){openModal('INSPECT','Inspect Asset ID','<div class="modal-form"><input id="inspectId" inputmode="numeric" placeholder="123456789"><div class="modal-actions"><button class="secondary-btn" id="inspectCancel">Cancel</button><button class="primary-btn" id="inspectGo">Inspect</button></div></div>');$('inspectCancel').onclick=closeModal;$('inspectGo').onclick=()=>{const id=$('inspectId').value.trim();closeModal();inspect(id)};$('inspectId').focus()}
function updateButton(){const n=$('assetName').value.trim();const ok=state.files.length>0&&n.length>=3&&n.length<=50&&!state.uploading;$('uploadAll').disabled=!ok}
const fileInput=$('fileInput'),dropzone=$('dropzone');
fileInput.addEventListener('change',e=>{addFiles(e.target.files);e.target.value=''});dropzone.addEventListener('click',e=>{if(e.target===fileInput)return;fileInput.click()});['dragenter','dragover'].forEach(t=>dropzone.addEventListener(t,e=>{e.preventDefault();dropzone.classList.add('drag')}));['dragleave','drop'].forEach(t=>dropzone.addEventListener(t,e=>{e.preventDefault();dropzone.classList.remove('drag')}));dropzone.addEventListener('drop',e=>addFiles(e.dataTransfer.files));document.querySelectorAll('.type-card').forEach(b=>b.addEventListener('click',()=>updateType(b.dataset.type)));
$('uploadAll').onclick=startUpload;$('retryFailed').onclick=retryFailed;$('clearQueue').onclick=clearQueue;$('pauseQueue').onclick=()=>{state.paused=!state.paused;$('pauseQueue').textContent=state.paused?'Resume':'Pause';if(!state.paused)pump()};$('assetName').addEventListener('input',()=>{renderQueue();updateButton()});$('nameTemplate')?.addEventListener('input',renderQueue);$('concurrency')?.addEventListener('change',e=>state.concurrency=Math.min(5,Math.max(1,Number(e.target.value||2))));$('creatorType').addEventListener('change',()=>$('groupField')?.classList.toggle('hidden',$('creatorType').value!=='group'));$('toggleAdvanced')?.addEventListener('click',()=>$('advancedPanel')?.classList.toggle('hidden'));$('assetSearch').addEventListener('input',e=>{state.search=e.target.value;renderHistory()});document.querySelectorAll('.history-tab').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.history-tab').forEach(x=>x.classList.remove('active'));b.classList.add('active');state.filter=b.dataset.filter;renderHistory()}));$('exportManifest').onclick=exportManifest;$('clearHistory').onclick=()=>{localStorage.removeItem('roblox_asset_hub_history');renderHistory()};$('scrollUpload').onclick=()=>$('uploadWorkspace').scrollIntoView({behavior:'smooth'});$('openInspectorTop').onclick=inspectPrompt;$('quickInspect').onclick=inspectPrompt;$('quickLua').onclick=exportLua;$('quickManifest').onclick=exportManifest;$('newWorkspace').onclick=newWorkspace;$('switchWorkspace').onclick=switchWorkspace;$('manageWorkspace').onclick=switchWorkspace;$('closeInspector').onclick=()=>$('inspectorBody').innerHTML='<div class="inspector-empty">Choose an asset from the library.</div>';$('modalClose').onclick=closeModal;$('modal').addEventListener('click',e=>{if(e.target.classList.contains('modal-backdrop'))closeModal()});$('themeToggle').onclick=()=>document.body.classList.toggle('compact');document.addEventListener('keydown',e=>{if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='k'){e.preventDefault();$('assetName').focus()}if(e.key==='Escape')closeModal()});
refreshWorkspace();updateType('auto');updateButton();renderPreviews();renderQueue();renderHistory();